<% provide(:title, "Create New User") %>
<h1>New user</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
		<%= form_for(@user) do |f| %>	
			<%= render 'fields', f: f %>
		  <%= f.submit "Create User", class: "btn btn-lg btn-primary" %>
		<% end %>
	</div>
</div>

<hr/>

<%= link_to 'Back', users_path %>
