<% provide(:title, "Create New Tester") %>
<h1>New Tester</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
		<%= form_for(@user) do |f| %>	
			<%= render 'fields', f: f %>
		  <%= f.submit "Take Test", class: "btn btn-lg btn-primary" %>
		<% end %>
	</div>
</div>

<hr/>

<%= link_to 'Back', users_path %>
